# SealedSecret for PostgreSQL credentials
# Generate this using kubeseal:
# 
# kubectl create secret generic postgres-secrets \
#   --namespace=zumba-production \
#   --from-literal=POSTGRES_PASSWORD=your-production-password \
#   --from-literal=DB_POSTGRESDB_PASSWORD=your-production-password \
#   --dry-run=client -o yaml | \
# kubeseal --controller-namespace=sealed-secrets --format=yaml > postgres-secrets.yaml

apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: postgres-secrets
  namespace: zumba-production
spec:
  encryptedData:
    # TODO: Replace with actual encrypted values using kubeseal
    POSTGRES_PASSWORD: AgA...  # Replace with sealed value
    DB_POSTGRESDB_PASSWORD: AgA...  # Replace with sealed value (same password)
