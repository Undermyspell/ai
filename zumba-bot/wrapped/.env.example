# Kopieren nach .env (gitignored) und Passwort eintragen.
# Passwort aus dem Cluster holen:
#   kubectl get secret postgres-secrets -n zumba-staging \
#     -o jsonpath='{.data.DB_POSTGRESDB_PASSWORD}' | base64 -d
DB_HOST=192.168.178.46
DB_PORT=5433
DB_USER=n8n
DB_PASSWORD=
DB_NAME=zumba
DB_SSLMODE=disable
