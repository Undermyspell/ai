# Upstream steht hier TimeoutStartSec=infinity. Ohne Netz/NTP wuerde der Boot
# damit haengen und k3s nie starten. Nach 180s aufgeben: die Unit faellt aus,
# time-sync.target wird trotzdem fertig und k3s startet (Wants= ist weich).
[Service]
TimeoutStartSec=180
