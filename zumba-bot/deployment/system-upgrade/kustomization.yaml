apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: system-upgrade

resources:
  # system-upgrade-controller — offizielles Rancher-Release, gepinnt.
  # Upgrade des Controllers selbst: Version hier bumpen (analog ArgoCD in
  # argocd/base/kustomization.yaml).
  # Releases: https://github.com/rancher/system-upgrade-controller/releases
  - https://github.com/rancher/system-upgrade-controller/releases/download/v0.20.1/crd.yaml
  - https://github.com/rancher/system-upgrade-controller/releases/download/v0.20.1/system-upgrade-controller.yaml
  # Unser Upgrade-Plan für k3s (die eigentliche Update-Ansage)
  - k3s-server-plan.yaml
