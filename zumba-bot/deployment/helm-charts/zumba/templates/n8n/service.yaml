apiVersion: v1
kind: Service
metadata:
  name: {{ include "zumba.fullname" . }}-n8n
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "zumba.n8n.labels" . | nindent 4 }}
spec:
  type: {{ .Values.n8n.service.type }}
  ports:
    - port: {{ .Values.n8n.service.port }}
      targetPort: 5678
      protocol: TCP
      name: http
  selector:
    {{- include "zumba.n8n.selectorLabels" . | nindent 4 }}
